export function ShopItem({itemName}) {
    return (
        <div>
            <img src="" alt="" />
            <span>{itemName}</span>
            <button onClick={add(itemName)}><span>Add</span></button>
        </div>
    )
}

function add(){

}